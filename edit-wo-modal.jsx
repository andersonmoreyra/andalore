function EditWOModal({order,onClose,onSaved}){const toast=useToast();const assets=Storage.getAssets();const techs=Storage.getTechnicians();const [form,setForm]=React.useState(order);if(!order)return null;const required=['titulo','equipamento','area','tipo','prioridade','status','solicitante'];const valid=required.every(k=>String(form[k]??'').trim()!=='');function patch(k,v){let next={...form,[k]:v};if(k==='equipamento'){const a=assets.find(x=>x.id===v);next.equipamentoNome=a?.nome||'';next.area=a?.area||next.area}if(k==='tecnico'){const t=techs.find(x=>x.id===v);next.tecnicoNome=t?.nome||''}setForm(next)}function save(){if(!valid){alert('Existem campos obrigatórios vazios.');return}Storage.updateWorkOrder(form.id,form);toast.push('OS atualizada');onSaved&&onSaved();onClose()}function del(){if(confirm('Deseja deletar esta OS?')){Storage.deleteWorkOrder(order.id);toast.push('OS deletada');onSaved&&onSaved();onClose()}}return <div className="modal-back"><div className="card modal"><div className="modal-head"><h2>Editar {order.id}</h2><button className="btn secondary" onClick={onClose}>Fechar</button></div><div className="form"><div className="field"><label>Título *</label><input value={form.titulo} onChange={e=>patch('titulo',e.target.value)}/></div><div className="row"><div className="field"><label>Equipamento *</label><select value={form.equipamento} onChange={e=>patch('equipamento',e.target.value)}>{assets.map(a=><option key={a.id} value={a.id}>{a.codigo} - {a.nome}</option>)}</select></div><div className="field"><label>Área *</label><input value={form.area} onChange={e=>patch('area',e.target.value)}/></div></div><div className="row"><div className="field"><label>Tipo *</label><select value={form.tipo} onChange={e=>patch('tipo',e.target.value)}><option>corretiva</option><option>preventiva</option><option>preditiva</option><option>melhoria</option></select></div><div className="field"><label>Prioridade *</label><select value={form.prioridade} onChange={e=>patch('prioridade',e.target.value)}><option>baixa</option><option>media</option><option>alta</option><option>critica</option></select></div></div><div className="row"><div className="field"><label>Status *</label><select value={form.status} onChange={e=>patch('status',e.target.value)}><option>aberta</option><option>em execução</option><option>pausada</option><option>concluída</option></select></div><div className="field"><label>Técnico</label><select value={form.tecnico} onChange={e=>patch('tecnico',e.target.value)}><option value="">Sem técnico</option>{techs.map(t=><option key={t.id} value={t.id}>{t.nome}</option>)}</select></div></div><div className="row"><div className="field"><label>Estimativa</label><input type="number" value={form.estimativa} onChange={e=>patch('estimativa',e.target.value)}/></div><div className="field"><label>Decorrido</label><input type="number" value={form.decorrido} onChange={e=>patch('decorrido',e.target.value)}/></div></div><div className="field"><label>Solicitante *</label><input value={form.solicitante} onChange={e=>patch('solicitante',e.target.value)}/></div><div className="actions"><button className="btn" disabled={!valid} onClick={save}>Salvar</button><button className="btn danger" onClick={del}>Deletar</button></div></div></div></div>}
