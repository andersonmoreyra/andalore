function PartsManager(){const toast=useToast();const [items,setItems]=React.useState(Storage.getParts());const [form,setForm]=React.useState({codigo:'',nome:'',estoque:0,estoqueMinimo:0,localizacao:'',fornecedor:'',preco:0});const valid=form.codigo&&form.nome;function refresh(){setItems(Storage.getParts())}function save(){if(!valid){alert('Código e nome são obrigatórios.');return}Storage.addPart(form);setForm({codigo:'',nome:'',estoque:0,estoqueMinimo:0,localizacao:'',fornecedor:'',preco:0});refresh();toast.push('Peça salva')}return <div className="grid cols-2"><div className="card form"><h3>Nova peça</h3>{['codigo','nome','estoque','estoqueMinimo','localizacao','fornecedor','preco'].map(k=><div className="field" key={k}><label>{k}{['codigo','nome'].includes(k)?' *':''}</label><input type={['estoque','estoqueMinimo','preco'].includes(k)?'number':'text'} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></div>)}<button className="btn" disabled={!valid} onClick={save}>Adicionar peça</button></div><div className="card table-wrap"><h3>Estoque</h3><table className="table"><thead><tr><th>Código</th><th>Peça</th><th>Estoque</th><th>Mín.</th><th>Local</th></tr></thead><tbody>{items.map(p=><tr key={p.id}><td>{p.codigo}</td><td>{p.nome}<div className="tiny">{p.fornecedor}</div></td><td><StatusPill value={p.estoque<=p.estoqueMinimo?'baixo':p.estoque}/></td><td>{p.estoqueMinimo}</td><td>{p.localizacao}</td></tr>)}</tbody></table></div></div>}
