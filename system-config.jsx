function SystemConfig(){const toast=useToast();function backup(){const blob=new Blob([JSON.stringify(Storage.exportData(),null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='backup-marviops.json';a.click();URL.revokeObjectURL(a.href)}function restore(e){const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=()=>{Storage.importData(JSON.parse(r.result));toast.push('Backup importado')};r.readAsText(f)}function reset(){if(confirm('Resetar dados locais?')){Object.values(Storage.KEYS).forEach(k=>localStorage.removeItem(k));Storage.initializeDefaultData();toast.push('Dados reiniciados')}}return <div className="grid cols-2"><div className="card"><h3>Backup e dados</h3><div className="actions"><button className="btn" onClick={backup}>Exportar backup JSON</button><label className="btn secondary">Importar JSON<input type="file" accept="application/json" onChange={restore} hidden/></label><button className="btn danger" onClick={reset}>Resetar</button></div></div><ChecklistManager/></div>}
