function AssetTree({assets,onSelect}){const groups=assets.reduce((acc,a)=>{(acc[a.area]=acc[a.area]||[]).push(a);return acc},{});return <div className="asset-tree">{Object.entries(groups).map(([area,list])=><div key={area}><strong>{area}</strong><ul>{list.map(a=><li key={a.id}><button className="btn secondary" onClick={()=>onSelect&&onSelect(a)}>{a.codigo} • {a.nome}</button></li>)}</ul></div>)}</div>}
