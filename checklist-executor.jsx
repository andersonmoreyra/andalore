function ChecklistExecutor({order}){const toast=useToast();const templates=Storage.getChecklistTemplates();const [tpl,setTpl]=React.useState('');const [checked,setChecked]=React.useState({});const template=templates.find(t=>t.id===tpl);function save(){if(!tpl){alert('Selecione um template.');return}Storage.addChecklist({workOrderId:order.id,templateId:tpl,respostas:checked,status:'concluído'});toast.push('Checklist registrado')}return <div className="card form"><h3>Executar checklist</h3><div className="field"><label>Template</label><select value={tpl} onChange={e=>setTpl(e.target.value)}><option value="">Selecione</option>{templates.map(t=><option key={t.id} value={t.id}>{t.nome}</option>)}</select></div>{template&&template.itens.map((it,i)=><label key={i}><input type="checkbox" checked={!!checked[i]} onChange={e=>setChecked({...checked,[i]:e.target.checked})}/> {it}</label>)}<button className="btn" disabled={!tpl} onClick={save}>Salvar checklist</button></div>}
