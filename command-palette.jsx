function CommandPalette({setScreen}){const [open,setOpen]=React.useState(false);const [q,setQ]=React.useState('');React.useEffect(()=>{const h=e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();setOpen(true)}};window.addEventListener('keydown',h);return()=>window.removeEventListener('keydown',h)},[]);const cmds=[['dashboard','Abrir dashboard'],['orders','Abrir ordens'],['team','Abrir equipe'],['requests','Abrir solicitações'],['stock','Abrir ativos e peças'],['settings','Abrir configurações']].filter(c=>c[1].toLowerCase().includes(q.toLowerCase()));if(!open)return null;return <div className="palette" onClick={()=>setOpen(false)}><div className="card palette-box" onClick={e=>e.stopPropagation()}><div className="field"><input autoFocus placeholder="Digite um comando..." value={q} onChange={e=>setQ(e.target.value)}/></div><div className="palette-list">{cmds.map(c=><button key={c[0]} onClick={()=>{setScreen(c[0]);setOpen(false)}}>{c[1]}</button>)}</div></div></div>}
