function LoginScreen({onLogin}){const [email,setEmail]=React.useState('anderson@marvi.com');const [password,setPassword]=React.useState('admin123');const [err,setErr]=React.useState('');function submit(e){e.preventDefault();const u=Auth.login(email,password);if(!u){setErr('E-mail ou senha inválidos');return}onLogin(u)}return <div className="login"><form className="card form" onSubmit={submit}><div className="brand"><div className="brand-mark">M</div><div><h2>MarviOps CMMS</h2><div className="muted">Gestão de manutenção industrial</div></div></div><div className="field"><label>E-mail</label><input value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>Senha</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></div>{err&&<div className="pill crit">{err}</div>}<button className="btn">Entrar</button><div className="tiny">Acesso: anderson@marvi.com / admin123</div></form></div>}
